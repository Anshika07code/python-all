import tkinter as tk
import random

def roll():
    label.config(text=f"Rolled: {random.randint(1,6)}")

root = tk.Tk()
root.title("Dice Roller")
btn = tk.Button(root, text="Roll Dice", command=roll)
btn.pack()
label = tk.Label(root, text="")
label.pack()
root.mainloop()
